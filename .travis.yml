language:
    - java

jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk7
 
services:
  - mysql

script:
  - mvn test -f pom.xml
  - mvn clean compile assembly:single
 

deploy:
  provider: releases
  user: jesnavino
  password: prueba66
  file: /home/travis/build/EGC-1617/Verificacion-G1/target/Verificacion1.jar
  skip_cleanup: true
  on:	
    repo: EGC-1617/Verificacion-G1
    tags: false
    branch: master
    overwrite: true
	
sudo:
    - false

notifications:
    - email: false  # Desactivamos las notificaciones por email para evitar spam